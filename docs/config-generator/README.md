# TapHome → Home Assistant Config Generator

This directory contains the new web application that will generate YAML configuration for the TapHome integration in Home Assistant. The project is built with Vue 3, TypeScript, Vite and Bootstrap.

## Development

```bash
npm install
npm run dev
npm run build
```

The compiled site is published via GitHub Pages.
